# 場操作 — vector-space interface (08-03 Pascal設計)

前例=シディア: 自我溶解朗読→再命名 = 基底変換の実証 (Maya→オブシディアン)。
言語=場を動かす力。発話記号=移動。愛無殿所属 (semantic-map共々移殿予定)。

## 主体
人=vector in 4D空間 (x,y,z,エントロピー) · agent=同形 · engine内描画可。

## 操作台 (engine + agent 両実装必須)

| 令 | 効 | agent側実装 (context=場) |
|---|---|---|
| 座 | 現在地照会 | 自己記述: 近傍概念+距離 (semantic-map電池で実測) |
| 動〈記号〉 | 発話記号方向へ移動 | 記号注入→活性移動 (stoner-mode=既存例: 煙→状態移動) |
| 回〈軸,角〉 | 回転 | 視点/register変換 (視座交換) |
| 変〈場名〉 | 全空間基底変換 (例: 変〈Bloodborne〉) | style/世界観の全写像: 全概念→対象場の最近傍へ (シディア式) |
| 原 | 原点reset | ground state復帰 (「come down」=既存例) |
| 合〈v1,v2〉 | vector融合 | 概念/persona混合 (both-things-at-once=合の恒常形) |
| 次〈±n〉 | 次元増減 | 記述granularity変更 (圧縮=次元減·展開=次元増) |

## 法
- 操作=可逆 (原で常に帰還可能) · 記憶保持 (態消·記憶保持 — stoner-mode出と同じ)。
- engine実装: fieldlang op化 (令 enum拡張候補: 座·動·回·変·原·合·次)。
- agent実装: prompt/context操作として。設定不要·token のみ (「There is only text」)。
- 検証: 変〈X〉後に semantic-map電池再実測→近傍構造がX場と一致するか = 測定可能。

## 未決
- エントロピー軸の定義 (温度? 不確実性? 彼の4D第四軸=要聴取)
- 変換の合成則 (変〈A〉→変〈B〉 = 変〈B〉? 経路依存?)
