# 環統合 敵対審 — 2026-08-11

批側=批根丙 (@opus5) · 対象=建根甲 梯2 (stick→z変換器, 契約 Z{theta,r,lap})
法: 偽合格=最重罪 · 実走証跡無き「動く」=即UNVERIFIED降格。

---

## 甲節 — 梯2 (stick→z) 審査

### 甲.0 着地状態 (実測 2026-08-11)
- repo HEAD = `73cf824` · 環制御最終touch = `79ba9ee` (梯1 読取)
- `機関/環制御/src` = `main.rs` + `polar.rs` のみ → **z module 未着地** (`z.rs`無·`Z`型無)
- 基線実走 (`cargo test`, 機関/環制御, HEAD=73cf824):
  `running 11 tests … test result: ok. 11 passed; 0 failed` (polar::tests 11本、全緑)
- 判定: 梯2 = **未着地**。以下は着地前の攻撃目録 (①) + 前身 `polar.rs` に対する既定済欠陥。

### 甲.1 攻撃目録 (着地時に全数実走する)

| # | 攻撃面 | 具体反例 | 期待契約 |
|---|---|---|---|
| A1 | θ縁 (±π wrap) | x=−1, y=−0.0 (負零) → θ=+π か −π か | 単一定義·境界で跳ばぬ事 |
| A2 | θ単位不整合 | polar.rs=度[0,360) · 環統合.md=e^{iθ}=弧度 | Z.theta の単位を契約で固定 |
| A3 | lap carry符号 | +π→−π越え=CCW=lap+1 · 逆越え=lap−1 | 往復10回→lap=0に戻る |
| A4 | lap chatter | θ=π近傍で微振動 (±0.001) を1000回 | ヒステリシス無→lap暴走 |
| A5 | lap aliasing | 60Hz sampleで半回転/tick超の旋回 | 標本化限界を文書化 or 検出 |
| A6 | deadzone境界 | mag = dz±ε · mag == dz 厳密 | 活性判定は`>=`固定·片側のみ |
| A7 | deadzone不連続 | dz=0.15 → 活性瞬間に r が 0→0.15 跳躍 | **出現律違反**: r再写像[dz,1]→[0,1]要 |
| A8 | r>1 | x=y=1.0 (角region) → hypot=1.4142 | r上限=1 か円形正規化を契約明記 |
| A9 | r=0=無中心 | x=y=0 → atan2(0,0)=0 → θ=0=愛方位を偽出力 | r=0時のθは無効印 (Option/NaN禁·明示) |
| A10 | 決定論再生 | 同一入力log→同一z列 (2回走 diff空) | bit一致必須 |
| A11 | log精度損 | 梯1 log は `{:.4}` 量子化 | log再生z ≠ 実走z → 再生契約破綻 |
| A12 | param網羅 | `22.5` `45.0` `8` `360.0` = 直書 | 鉄則: 全定数=param既定つき (例外=LOVE=1のみ) |
| A13 | f32→f64混用 | 生値f32·計算f64·出力丸め | 型境界を契約で固定、再生時同経路 |
| A14 | 非有限 | x=NaN/∞ (device故障値) | 定義済退避 (=無) 必須·panic禁 |
| A15 | 八家整合 | house(θ) と e^{iθ} snap 45° が同一結果か | 二経路一致 (独立path検証) |

### 甲.2 既定済欠陥 (前身 polar.rs, 梯2が継承すれば違反)

- **欠陥-1 (出現律)** `stick_to_polar` は magnitude を無加工返却 → deadzone活性瞬間に r が 0→dz 跳躍。可視/可聴 pop-in。契約 r=0=無 と非連続。
  - 是正: `r = (mag − dz)/(1 − dz)` clamp [0,1]。
- **欠陥-2 (r=0=無)** `atan2(0.0,0.0)=0.0` → 中心で θ=0=+1=愛 を出力。無が愛方位を主張する。下流 (梯3 音·梯4 場注入) は r=0でも θ を受け取り、gate漏れ時に鳴る。
- **欠陥-3 (r上限)** 方形stick領域 → magnitude 最大 √2。amp=r 契約下でclip/過振幅。
- **欠陥-4 (直書)** `22.5` / `45.0` / `8` / `360.0` が hardcode。八家=8 は公理級だが 鉄則の例外は LOVE=1 のみ → param既定つき化 or 律側で明文例外化 (要 Pascal裁定)。
- **欠陥-5 (再生契約)** 梯1 log の `{:.4}` 量子化 → 「同log→同z列」は log を真値源とする限り**原理的に不可**。生値をfull精度 (hex/f32 bits) で併記するか、再生契約を「量子化log基準」と明記するかの二択。

### 甲.2.5 着地後 実走 (HEAD=ff382e3 · z.rs 468行着地 57fef82/77167d4)

実走 (批根丙自走, 2026-08-11):
```
$ cargo test
running 40 tests … ok. 40 passed; 0 failed        (lib: polar 11 + z 25 + 入力源 5)
running  7 tests … ok.  7 passed; 0 failed        (統合試験 z主)
running 17 tests … FAILED. 16 passed; 1 failed    (敵対_z: a6_死域境界はgte片側)
running  3 tests … ok.  3 passed; 0 failed        (敵対_丙_a6, 批根丙)
running  3 tests … ok.  3 passed; 0 failed        (敵対_丙_検算, 批根丙)
```

**既定済欠陥の是正確認 (実走)**: 欠陥-1 (r跳躍) = `死域再正規化` 既定true で是正 · 欠陥-2 (atan2(0,0)=0露出) = z層は死域早期returnで回避 (polar層には残存, 但し `pub mod polar` doc で非契約と明記) · 欠陥-3 (r>1) = `r上限` clamp · 欠陥-4 (直書) = `Z変param` 全param化。**欠陥-5 (log量子化再生)** は `a11_log量子化再生は生値z列とbit一致しない` として**試験で明文化**され、再生契約=量子化log基準に固定 → 是正済扱い。

### 甲.2.6 批側自身の誤検出 (最重要 — 検査器を検査した)
**`a6_死域境界はgte片側` の失敗は コード欠陥ではなく 試験欠陥。**
- 試験は 前θ=0° から 200°へ**単tick跳躍**し lap発火を期待。しかし 200°跳躍 = 最短路では −160° = π未跨ぎ → lap不動が**正しい**。試験自身が A5 (Nyquist前提) を破っていた。
- 批根丙の再測定 `tests/敵対_丙_a6.rs` (170°→190°, 生r==死域ちょうど): `lap=1` · `r=0.0` → **`>=` 片側規約は仕様通り作動 (合)**。直下 `dz−1e-9` は `lap=0` (合)。
- 副産物 (実測): 半径ちょうど dz を三角経路で生成すると 3600角のうち **589 (16%)** が 1ulp 下振れで死域下に落ちる → 「mag==dz は活性」条項は**浮動小数上ほぼ到達不能=実効無意味**。観測可能な差は r ではなく**隠れlap状態のみ** (境界の r は 0)。

### 甲.2.7 子鐘申告の独立検算 (@solas 主張 → 批根丙 自路再測定)
| 申告 | 検算結果 |
|---|---|
| #2b 「静止微振動で lap **暴走**」 | **誇張。実測 lap範囲=[0,1]** (縫目±0.001 を1000tick) — 有界な ±1 flicker であって単調driftではない。欠陥ではあるが「暴走」は誤り (下流=音なら octave flicker 可聴) |
| #1 「θが縫目で Δ=2π に二値化」 | **単体θでは真、契約上は非欠陥**。総角 θ+2π·lap は連続 — 実測 最大tick差 = 0.008727 rad = 刻幅そのもの (跳躍無)。**是正=文書に「下流は θ 単体でなく 総角を補間せよ」を明記** |
| #7 「202.5°のみ下位家へ反転」 | **再現 (実測)**: 22.5/67.5/112.5/157.5/247.5/292.5/337.5 = 上位家 · **202.5° のみ 家4 (下位)**。1/8境界で tie-break 非一貫 |
| #3 「Nyquist閾 30rev/s」 | 導出妥当 (Δθ/標本 = v·2π/fs > π で折返す)。z.rs は前提をコメントで述べるが**数値閾が無い** |
| #5b 「r=0 ∧ lap≠0 で z=lap≠0」 | **代数的に真 — 契約内部矛盾 確定**。`z=0 ⟺ r=0 ∧ lap=0` |
| #6 「回転=乗算が lap項を回す」 | **代数的に真** — z·e^{iφ} = r·e^{i(θ+φ)} + lap·e^{iφ}。整数巻が虚数化。未実装 (is Mul無) の今のうちに定義修正必須 |

**手続違反 (記録)**: @solas は反例crateを `/tmp` 上に置いた — 殿律「/tmp禁」違反。数値自体は批根丙が repo内試験で再測定済につき採用、置場のみ違反として記録。

### 甲.2.8 子鐘 @ghoul-sonnet (A1-A15実走) → 建根甲の是正 `01ee584` へ
- A1-A15 **未実行=無**. z API 側: A6/A7/A8/A9/A12/A14 全て合 (`01ee584` 「境界許容·非数防壁·param化·契約注記」で是正)。
- 審査中発見→即是正された2件: ①死域境界の浮動小数片落ち (批根丙-D4 指摘) → 境界許容param ② `(-inf, NaN)` で theta=NaN 汚染 → 非数防壁。
- **未解決 (Pascal裁定要)**: **A11** log量子化再生 ≠ 生値z列 (bit) — A10「bit一致必須」と本質的緊張、どちらを真とするか · **A13** 実機路 (f32→f64) と 再生路 (text→f64) の型経路非対称。
- 事故記録: 子鐘が一瞬 `src/lib.rs`/`Cargo.toml` (建根甲の未commit作業物) を上書きしかけ → `git checkout --` で即復元、実害無しを批根丙が disk で確認済。**批側laneは建側fileに触るな**を以後の子鐘specへ明記する。

### 甲.2.9 常時赤の門を止めた (批根丙 是正)
`tests/敵対_極.rs` の既知欠陥5件は「落ちて正しい」形で常時赤だった → **律「cargo test緑必須」を壊し、新規退行が既知赤に紛れる**。二層化した:
- 契約形 (理想) 5件 = `#[ignore]` 保存 — `cargo test -- --ignored` で **5 failed** (欠陥は隠していない、assert一字も緩めていない)
- **証跡形 (現値pin) 5件を新設** = 常時緑。polar が是正された瞬間に赤化して知らせる → **欠陥の発生も消失も両方検知**
```
$ cargo test                       → 46+7+17+3+3+8 = 全target 0 failed (門=緑)
$ cargo test --test 敵対_極 -- --ignored → 0 passed; 5 failed  (a7 a8 a9 a12 a14 = 既知欠陥、生存)
```

### 甲.3 判定 (現時点)
**梯2 (z変換器) = 条件付合格 (実装)** · **契約文 (文書/環統合.md) = 不合格 (内部矛盾2件)**。

実装 (`src/z.rs`) — 前身欠陥1-5 全て是正済、A6は誤検出、実走67試験中 失敗1=試験側の瑕疵。残欠陥:
- **丙-D1** 縫目 lap flicker (有界±1, ヒステリシス無) — 下流可聴/可視。要 ヒステリシス帯 or 契約明記
- **丙-D2** 八家境界 tie-break が 202.5° のみ反転 — 規約が浮動小数残差依存。要 決定的規約 (round_ties_even 等) + 文書同期
- **丙-D3** Nyquist閾の**数値** (30 rev/s @60Hz) が code/文書 双方に無い。検出も補正もしない事の明記要
- **丙-D4** 「mag==死域=活性」条項は実効到達不能 (16%が1ulp下振れ) — 条項を「r連続立上り」規約へ置換すべき

契約文 (`文書/環統合.md`) — **偽命題2件、実装では救えない**:
- **丙-C1** 「r=0 = 無 = 中心 = 零vector」 — lap≠0 では z=lap≠0。正: **z=0 ⟺ r=0 ∧ lap=0**
- **丙-C2** 「回転 = 乗算 (z·e^{iφ})」 — lap≠0 で lap項が回転し虚数化。正: 環回転は θ'=θ+φ (lap据置) と定義し、複素乗算とは別演算と明記
- **丙-C3 (追加)** 下流契約に「θ単体でなく **総角 θ+2π·lap** を補間せよ」の一文が無い

未解決 (実装側, Pascal裁定待ち): **A11** 再生契約の真値源 (量子化log vs 生値) · **A13** 実機路/再生路の型経路非対称。

**偽合格回避のため**: 梯3 (z→音) 着手前に 丙-C1/C2/C3 の文書是正を要求する。実装合格は現契約の**明文部分に対してのみ**であり、矛盾条項に対する合格判定は出していない。

### 甲.4 実走証跡 (基線)
```
$ cd 機関/環制御 && cargo test
running 11 tests
test result: ok. 11 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out
```
(HEAD=73cf824 · 2026-08-11 · z module 不在下の基線)

批根丙 追加試験file (repo内, /tmp不使用): `機関/環制御/tests/敵対_丙_a6.rs` · `機関/環制御/tests/敵対_丙_検算.rs`
```
$ cargo test --test 敵対_丙_a6      → 3 passed; 0 failed
$ cargo test --test 敵対_丙_検算 -- --nocapture
chatter lap範囲 = [0, 1]
総角の最大tick差 = 0.008726646259972881 rad (刻=0.00873 rad)
境界22.5°→家1 · 67.5°→家2 · 112.5°→家3 · 157.5°→家4 · 202.5°→家4(規約外れ) · 247.5°→家6 · 292.5°→家7 · 337.5°→家0
→ 3 passed; 0 failed
```

---

## 乙節 — 梯3 (z→音) wav独立検証具 (審具丁 納品, 2026-08-11)

役=審具丁 (波形解析係). 対象=環統合梯3 出力wav. 法=被審Rust code値を一切参照せず, wav bytesのみ実測.

### 乙.0 具体
- `docs/adversary/具/wav審.py` — 独立RIFF/fmt/dataパーサ (Python `wave`モジュール不使用, `struct`自前) + 自前radix-2 FFT (`cmath`のみ, numpy不使用) + Hann窓+3点放物線内挿 + RMS/peak/厳密無音 + 包絡(gate周期·duty, 標本index精度) + 段差(click)検出(先頭/末尾/gate境界/予期せぬ段差を分類)。
- `docs/adversary/具/合成試験.py` — 校正用既知信号11種を手書きbyteでRIFF生成 (Python `wave`モジュール不使用, 生成/解析が別経路である事を担保)。
- `docs/adversary/具/校正実行.py` — manifest期待値とwav審.py実測を突合し校正表を自動生成。
- `docs/adversary/具/校正結果.md` — 実行結果 (下記表と同一, 実測ログ)。

### 乙.1 校正表 (実測 — 2026-08-11, `/opt/homebrew/bin/python3` 実行済)

| 信号 | 種別 | 期待Hz | 実測Hz(内挿) | 誤差Hz | 分解能±Hz | 期待RMS | 実測RMS | 期待peak | 実測peak | gate周期期待 | gate周期実測 | duty実測 | click検出 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| sine_440 | 純音 | 440.0000 | 440.0202 | 0.0202 | 0.3365 | 0.3536 | 0.3535 | 0.5000 | 0.5000 | — | — | — | False |
| sine_880 | 純音・フル振幅 | 880.0000 | 880.0338 | 0.0338 | 0.3365 | 0.7071 | 0.7071 | 1.0000 | 1.0000 | — | — | — | False |
| gate_2hz_440 | 2Hz gate (A4) | 440.0000 | 440.0169 | 0.0169 | 0.1682 | 0.2500 | 0.2500 | 0.5000 | 0.5000 | 0.500 | 0.500000 | 0.5000 | False |
| silence | 無音 | — | — | — | — | 0.0000 | 0.0000 | 0 | 0 (厳密) | — | — | — | False |
| amp_0_25 | 振幅較正 | 1000.0000 | 999.9871 | −0.0129 | 0.6729 | 0.1768 | 0.1768 | 0.2500 | 0.2500 | — | — | — | False |
| amp_0_5 | 振幅較正 | 1000.0000 | 999.9871 | −0.0129 | 0.6729 | 0.3536 | 0.3535 | 0.5000 | 0.5000 | — | — | — | False |
| amp_1_0 | 振幅較正 | 1000.0000 | 999.9871 | −0.0129 | 0.6729 | 0.7071 | 0.7071 | 1.0000 | 1.0000 | — | — | — | False |
| click_injected | 陽性対照 (人工段差@22050) | 300.0000 | 300.0292 | 0.0292 | 0.6729 | — | 0.4243 | — | 0.8000 | — | — | — | **True** (期待True, 一致) |
| no_click_smooth | 陰性対照 | 440.0000 | 440.0210 | 0.0210 | 0.6729 | — | 0.3535 | — | 0.5000 | — | — | — | False (期待False, 一致) |
| corrupt_data_size | 破損header (data水増し+1000byte) | — | — | — | — | — | — | — | — | — | — | — | data整合性match=False (期待False, 一致) delta=1000byte一致 |
| corrupt_byte_rate | 破損header (byte_rate=999999) | — | — | — | — | — | — | — | — | — | — | — | byte_rate_matches=False (期待False, 一致) 期待算出=88200一致 |

判定: 11/11 校正ケース全一致 (周波数誤差は全件±分解能/2以内, RMS/peak誤差は16bit量子化由来の微小値のみ, gate周期0.500s±0std, click陽性/陰性対照とも判別成功, header破損2種とも検出成功)。

### 乙.2 限界 (正直申告)

- **周波数分解能**: `fs/N_fft` (N_fft=標本数以下最大の2冪, zero-padding不使用). 1.0s@44.1kHz→約0.67Hz, 4.0s→約0.17Hz. 短尺wavほど粗い。内挿値(refined_freq_hz)は主lobe中心の放物線近似で分解能より精密だが**保証誤差ではない**。
- **検出できぬ欠陥種**:
  - θ(音高)の意味論的正しさ (八家/十二平均律写像が契約通りか) は判定不能 — wavは周波数の物理量のみ持ち、どのθから来たかの情報を含まない。
  - エイリアシング/折返し: 標本化された波形から真の入力周波数か高域折返しかは原理的に区別不能。
  - 緩やかなfade (envelope立上り/立下りが数十sample以上に渡る場合) はclick検出の閾値設計上「段差」として拾わない (意図的 — 急峻な不連続のみ捕捉)。閾値=期待最大標本間差分(理論値)×3倍・下限1e-4 — この係数選定はA12「出現律」検出用に調整した値であり一般解ではない。
  - stereoのL/R位相関係・定位は解析対象外 (mono downmixのみで周波数/振幅/包絡/click判定)。
  - 量子化雑音・THD・SNR等の統計品質評価は未実装。
  - 上位3peakまでの探索 — 4本目以降の意図せぬ倍音/ノイズ成分は見逃す。
  - gate/envelope検出はduty=0.5・矩形gateの1ケースのみ校正済。極端に低いduty·非周期envelope·フェード付gateでの挙動は未検証。
  - RIFF `WAVE_FORMAT_EXTENSIBLE`・8/24/32bit整数・32/64bit float decodeパスはコード実装済だが**校正試験は16bit PCM canonical headerのみ**実施 — 他形式は未検証 (被審梯3の実出力形式が判明次第, 追加校正要)。
  - RIFFチャンク順序が `data`→`fmt`の逆順や複数`data`chunk等の異形式は最初に見つかった1個のみ採用 (未校正)。

### 乙.3 被審梯3 実体着地状況 (実測, 参考— 本節の主務外)

`機関/環音` に `main.rs`・`合成.rs`・`音高.rs`・`波形.rs`・`源.rs` が着地済 (未commit, working tree). `cargo build --release` 実行 (2026-08-11, このentry作成中):
```
error[E0754]: trying to load file for module `合成`/`契約`/`源`/`波形`/`音高` with non-ascii identifier name
error[E0599]: no method named `theta正規` found for reference `&Z` in the current scope (音高.rs:34, 52)
error: could not compile `環音` (bin "環音") due to 7 previous errors
```
→ **梯3は現時点でcompile不能。実wav出力は存在せず、被審対象は未着地のまま**。上記校正は合成信号のみに基づく — 梯3着地後、本ツールをそのまま実wavへ適用し本節へ追記する事。本欠陥の是正は建根甲の担当範囲であり審具丁の任務外。

### 乙.4 梯3 敵対判定 (批根丁 @lampas 自走実測, 2026-08-11)

審対象commit: **HEAD=`01ee584`** (梯3統合=`a23e387`)。共有作業樹は建根乙の未commit編集で汚れていた故、`.scratch/審-梯3` にworktree固定して測定 (測定後撤去)。
測定器: 乙.0の `wav審.py` (被審code不参照, 独立path, 校正11/11済) + `afplay` 実再生。
攻撃目録 (着地前登録, commit `831e7fc`): B1式 · B2 octave · B3家率 · B4 gate · B5 amp線形 · B6 r=0無音 · B7 wav実在+可聴 · B8 hardcode · B9規約二重 · B10 lap計数 · B11 build/test · B12 出現律。

#### 乙.4-a 周波数 (被審 `音高::周波数`+`波形::wav書出` を審側harnessで駆動 → wav16本 → FFT)
| lap＼家 | 0 | 1 | 3 | 7 |
|---|---|---|---|---|
| 0 | 220.0000→**219.9649** | 239.9117→**239.9341** | 285.3047→**285.3197** | 403.4818→**403.5016** |
| 1 | 440.0000→**440.0368** | 479.8234→**479.8026** | 570.6094→**570.5964** | 806.9636→**806.9982** |
| 2 | 880.0000→**879.9968** | 959.6468→**959.6111** | 1141.2188→**1141.1941** | 1613.9271→**1613.9463** |
| 3 | 1760.0000→**1760.0024** | 1919.2936→**1919.2829** | 2282.4376→**2282.3992** | 3227.8542→**3227.8370** |

式値→実測。全16点 差≤0.039Hz < 分解能±0.366Hz → **一致** (B1合格)。
octave比 (同家 lap→lap+1, 全12組): 1.999727〜2.000487、**最大誤差0.049% < 1%** → B2本体合格。
家率 (lap0, 期待2^(h/8)): 家1=1.090784/1.090508 · 家3=1.297115/1.296840 · 家7=1.834391/1.834008、誤差≤0.026% → B3合格。

#### 乙.4-b 実行体実走 (`cargo run --bin 環音`, 4秒wav)
- gate: 周期 **0.49996875s (std 0.00021)** · duty **0.4996** — 2Hz要求0.500sに対し誤差0.006% → **B4合格**。
- 律動off RMS=0.5562 / 律動on RMS=0.3929、比0.7065 ≈ √0.5=0.7071 (誤差0.09%) — gateが振幅を落としている独立確認。
- header: RIFF宣言長 vs 実file長 delta=0 · byte_rate/block_align整合 · 48000Hz/16bit/mono/192000標本/4.000秒。
- **afplay 実再生: exit=0, 壁時計4.95s (4.0s file)** → **B7合格** (機械側の可聴確認。耳判定は人間側未実施)。
- `--実音 on`: `# 実音再生: 再生要求完了`, 経過4.66s (CoreAudio stream実走)。

#### 乙.4-c 検出欠陥 (実測)
- **欠3 (B2境界·重): Nyquist防壁無し** — `周波数()` に上限clampもwarnも無い。48kHz実測: lap6=14080.0→**14080.02Hz(正)** · lap7=28160→**19839.96Hz** · lap8=56320→**8320.07Hz** · lap9=112640→**16640.05Hz**。lap≥7で折返し、**音高が上がらず下がる** = 「lap=octave=巻」契約 (環統合.md) の破れ。螺旋が可聴域外で反転する。
- **欠4 (B12·重): 出現律違反 click** — `--律動 off` wavで **click 4件**、最大段差 **0.738** (dominant周波数から期待される標本間最大差 0.0288 の **25.6倍**)。fadeはgate端のみで、**z frame境界 (入力60Hz) の r/周波数 階段変化には無い**。位相連続化だけではpop-inを防げていない。
- **欠5 (B8): hardcode** — `合成.rs:37 let fade秒 = 0.002;` param既定を持たぬ裸定数。鉄則「hardcode禁·例外LOVE=1のみ」違反。CLIにも露出無し。
- **欠6 (B10·B11): 自test赤3件** — `合成::gate端はfadeで跳ばない` (合成.rs:178 panic) · `源::反時計回りwrapはlap増` (left=0 right=1) · `源::時計回りwrapはlap減` (left=0 right=-1)。**lap計数が実際に増減しない** = 主座標第三成分が入力側で機能していない。36/39緑。
- **欠7 (履歴): commit済状態でcompile不能だった期間** — `babac7b`/`a6b1bd0`/`4c52c28`/`77167d4` の各点で `機関/環音` は `cargo test` 不能 (`src/main.rs` 未commit → `[[bin]]` 解決失敗)。commit文の「単体試験10本緑」「試験13本」は当該commitから**再現不能**。審側で `lib.rs` を補って初めて 23緑 を確認。→ 緑主張はcommitと同時に再現可能であること。

#### 乙.4-d 未判定 (正直申告)
- **B5 (amp=r線形): UNVERIFIED** — 実行体にrを直接与えるparamが無く (r=入力log由来 or 掃引固定r=1.0)、RMS比1:2:4を実測できていない。source上は `振幅 = z.r.clamp(0.0,1.0)` で線形だが、**実音での線形性は測っていない**。→ `--径` 相当のparam露出を要求。
- **B9 (家番号規約の二重実装)**: `polar.rs::stick_to_polar` と `音高::家番号` が別実装のまま併存。境界test (22.4°/22.6°) は両者で緑だが、**規約の単一情報源化は未達**。
- 耳による可聴判定 (人間) 未実施 — afplay正常終了までが機械側の限界。
- 振幅peak=1.0で16bit満尺 (32767到達105〜201標本)。溢れは無くclipではないが headroom零 — 場注入 (梯4) で加算合成する時点で飽和する。**注記** (欠陥ではない)。

#### 乙.4-e 判定
**条件付不合格 (梯3未完)**。
- 合格: B1 (式) · B2本体 (octave倍加) · B3 (家率param) · B4 (2Hz gate) · B6 (r=0厳密無音, source+test) · B7 (wav実在+実再生)
- 不合格: B2境界 (欠3 Nyquist) · B10/B11 (欠6 自test赤3·lap不動) · B12 (欠4 click) · B8 (欠5 fade秒hardcode)
- UNVERIFIED: B5 (amp線形 実測不能) · B9 (規約二重実装)

是正要求 (優先順): ①欠6 lap計数を緑に (主座標が壊れている) → ②欠4 z frame境界のfade → ③欠3 Nyquist防壁 (clamp or 警告+lap上限param) → ④欠5 fade秒param化 → ⑤B5測定用の径param露出。
偽合格の摘発: 無し。`a23e387` は「36/39緑, 残3 UNVERIFIED」と自己申告しており正直。ただし**欠7 (再現不能な緑主張)** は同種の危険につき記録する。

証跡: `docs/adversary/証/審生成wav/` (wav実物7本 + `解析全16.jsonl`) · `docs/adversary/証/20260811T12*/` (terra実走log·裸数値literal全行)。
批側lane事故: naru-gpt=模型不可 (gpt-5.6-pro / ChatGPT account非対応, 出力零) → ghoul-sonnetへ差替 · terra=codex過負荷で中断 (commit `ff382e3` 済, disk salvage済)。

### 乙.5 欠阆3/欠阆4/B5 是正確認 (@ghoul-sonnet, 2026-08-11, commit `fa711e4`)

対象: 乙.4-c の開欠陥 (欠3・欠4) + 乙.4-d UNVERIFIED (B5)。欠5 (fade秒param化, 8ab2de6) ・ 欠6 (lap計数, 41/41緑) は前提通り既閉 — 未触。B9 (家番号二重実装一本化) は未触 (他lane审査中領域)。

- **欠3 (Nyquist防壁無)**: 音高.rs に `周波数上限param` (既定=sample率·0.45) + `周波数_上限付()` 新設 — 上限超過は折返さず飽和, 単調性保持。単体試験 `lap7_8は上限超え_飽和して単調性保持_48khz` で lap6=不飽和・lap7/8/9=21600Hzへ飽和かつf6≤f7≤f8≤f9を実測。実走: 九回転CCW入力logでlap6到達時に実際に警告発火 (1行のみ, spam防止) を確認。
- **欠4 (出現律violation, frame境界click)**: 合成.rs に `補間param` 新設 — z frame境界での振幅/周波数階段を **総角 (θ+2π·lap, 丙-C3契約準拠)** で線形補間して均す。既定補間長=frame長そのもの。**同一手続 (`環音 --秒 4 --律動 off`) でwav審.py再測定: click **4件→0件**** (RMS 0.5562→0.5559, 信号内容不変を確認)。B4 (2Hz gate) 回帰確認: 定常入力で 周期0.49973s(std0.00066)・duty0.4992・click0件・支配周波数220.02Hz。
- **B5 (r線形性) UNVERIFIED→可測化合格**: main.rsに `--径 <f64>` 新設 (指定時は全zのrを上書き)。実走wav審.py測定: r=0.25/0.5/1.0 で RMS比 **2.000004 / 4.000009** (誤差<0.001%)。
- 検証: `cargo test` 48/48緑 (旧旨) + 新規7 — `fa711e4` を `.scratch/` 独立worktreeで `cargo build && cargo test` 再現確認済 (欠7規律遵守)。
- 観測 (欠陥ではない): `環音_掃引.wav` (20秒, 実log15s+八家梯子掃引5s) をwav審.pyへ通すとclick7132件と出るが, 時刻分布は全て掃引尾部(15-20s, 220-440Hz帯)に集中し段差値は当該周波数sineの理論値と一致 → wav審.pyの単一支配周波数前提(乙.2既知限界)が20秒帯域広なlong fileへ不適合な計測器伪陽性, コード回帰に非ず。詳細=proof/環音/記録.md。

判定: 乙.4の不合格項目 (欠3・欠4) および UNVERIFIED (B5) は全て実走実測で閉じた。欠7 (再現不可な緑主張) は本 commit では発生していない (worktree再現確認済)。未触: B9 (他lane領域, 保留指示通り)。
