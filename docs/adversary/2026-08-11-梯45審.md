# 梯4/梯5 敵対審 — 2026-08-11

法: 偽合格=最重罪 · 実走証跡無き「動く」=即UNVERIFIED降格 · 被審code値を真値源にせぬ事 (独立path必須)。
一季exemplar = `docs/adversary/2026-08-11-環統合審.md`。

- **甲節** — 梯4 (z→場注入+haptic). 批側=**批根丙 (@opus5, 二季)**。目録id=`K*` (乙節 `C*` と衝突せぬ為)。
- **乙節** — 梯5 (mic→z, crate `機関/歌口`). 批側=**批根丁 (@lampas, 二季)**。以下。

---

## 甲節 — 梯4 (z→場注入 + haptic 2Hz帰路) 審査

批側=批根丙 (@opus5) · 規律: 批側laneは建側fileに触らぬ (一季 甲.2.8 事故) · /tmp禁 · 測定は `.scratch/` 固定worktree。

### 甲.0 着地状態 (実測 2026-08-11, HEAD=`aacc6e6`)
- `機関/` crate列 = 世界 世界審 内在 宇宙 宇宙-嵐修 宇宙-老化診断 小殿 環制御 環音 零 零審 → **梯4 crate 不在**
- `機関/環制御/src` = lib/main/polar/z/z主/入力源 — haptic出力路 **無** (`grep haptic|振動|rumble` = 無関係doc 1行のみ)
- 場側注入API候補 = `機関/宇宙/packages/field` (`ops.rs`/`phenomena.rs`/`world.rs`) 、環制御からの依存 **無**
- 判定: 梯4 = **未着地**。以下は着地前登録の攻撃目録 + 既に測った分。

### 甲.1 攻撃目録 (梯4 — 着地時に全数実走)

| # | 攻撃面 | 具体反例 | 期待契約 |
|---|---|---|---|
| K1 | 場保存則 (総量) | r=0 (無) 注入 → 場総量 Δ=0 厳密 | 無は場を動かさぬ |
| K2 | 場保存則 (符号) | θ と θ+π の対蹠注入を対で打つ → 総量Δ 相殺 (±ε) | 波紋=変位, 質量生成に非ず |
| K3 | 場保存則 (線形) | 同θで r=0.25/0.5/1.0 → Δ総量比 1:2:4 (又は非線形則を明文) | 振幅=r の場側検証 |
| K4 | 場保存則 (時間) | 注入後 N step 自由発展 → 保存量 drift < 明示閾 | 数値散逸を契約明記 |
| K5 | 出現律 (pop-in) | frame列差分 — 注入tickで \|ΔI\| が近傍frameの k倍を超えぬ事 | 出現=連続時間函数 (律) |
| K6 | 出現律 (消滅) | r: 1→0 を1tickで落とす → 場/画/haptic の即断禁 | 立下りも連続 |
| K7 | 総角規約 (丙-C3) | 縫目 (θ: +π→−π) 跨ぎ入力列 → 注入方向が 2π 跳ばぬ事 | 下流は **総角 θ+2π·lap** で補間 |
| K8 | 総角 再発狩り | 梯4 code 全体で `theta` 単体のlerp/差分を静的走査 | θ単体補間=契約違反 |
| K9 | haptic 2Hz 実測 | 出力指令列の時刻log → 周期 0.500s ±閾 | 環制御.md「haptic帰路=2Hz」 |
| K10 | haptic 実機 | DualSense 実接続で rumble 実走 · 無device時の退避 | 実機証跡無=UNVERIFIED |
| K11 | haptic 位相 | 場応答→haptic の遅延を実測·明文化 | 帰路である事 (開ループ禁) |
| K12 | A11 再生bit一致 | 梯1 log `{:.4}` 量子化 → 独立parser再計算と z再生log を突合 | 再生契約=量子化log基準の明文と一致 |
| K13 | A13 型経路 | 実機路 f32→f64 · 再生路 text→f64 の非対称 | 契約で固定、再生時同経路 |
| K14 | hardcode狩 | 梯4 全定数の param既定 (例外=LOVE=1のみ) | 鉄則 |
| K15 | param網羅 | 各param が CLI/構造体に露出し、既定値が文書と数値一致 | 文書/code 同期 |
| K16 | 決定論 | 同一入力log → 場digest 2回走 一致 | bit一致 |
| K17 | 飽和 | 梯3注記 (peak=1.0, headroom零) と場注入の加算合成 → clip | 溢れ処理を契約明記 |
| K18 | 非有限 | z に NaN/∞ (上流防壁を外した想定) | panic禁·定義済退避 |

### 甲.2 批側 独立検証具
- `docs/adversary/具/z再生審.py` — 梯1 log を**自前parser**で読み、契約 (環統合.md §主座標 + 出力log headerのparam) のみから z を**独立再実装** → 被審出力と突合 (K12/K13)。被審Rust code 不参照。
- (予定) `frame差分.py` — 場frame列の連続性検定 (K5/K6)。梯4 の出力形式確定後に完成させる。

### 甲.3 着地前 実走所見 (批根丙 自走, HEAD=`aacc6e6`)

#### 甲.3.1 【重】欠陥-丙4-1 — haptic は macOS gilrs では **無音の空振り** (K9/K10 先取り)
実測 (disk上のcrate源, `機関/環制御/Cargo.lock` 固定版 `gilrs 0.11.2` / `gilrs-core 0.6.8`):
```
gilrs-core-0.6.8/src/platform/macos/ff.rs:16
    pub fn set_ff_state(&mut self, _strong: u16, _weak: u16, _min_duration: Duration) {}   ← 本体空
gilrs-core-0.6.8/src/platform/macos/gamepad.rs:348
    pub fn is_ff_supported(&self) -> bool { false }
    pub fn ff_device(&self) -> Option<FfDevice> { Some(FfDevice) }   ← Noneでなく Some
```
→ **mac では ff指令が成功を返しつつ物理振動は零**。`ff_device()` が `Some` を返す為、存在確認codeも通る。
**偽合格の罠**: 梯4 が gilrs の ff API で haptic を実装すれば、自testも実走logも緑のまま **実機は一度も震えない**。
- 要求: haptic は gilrs 経由不可 → DualSense HID 出力report直書 (`hidapi`, USB=0x02 / BT=0x31) 等の別路必須。
- 要求: 「2Hz出た」の証跡は **指令log では不可** — 外部観測 (人手確認/録音/加速度) を要する。指令logのみの主張は **UNVERIFIED 固定**。
- 副次: `is_ff_supported()==false` を起動時に検査し、偽って動作させぬ事 (無対応の正直申告)。

#### 甲.3.2 K12/K13 (一季 A11/A13) — 独立経路で **完全一致 確認** (合)
```
$ python3 docs/adversary/具/z再生審.py proof/環制御/入力log.txt <z log>
入力TICK=900 · z行=900
θ 最大差=0 不一致行=0 · r 最大差=0 不一致行=0 · lap 不一致行=0 (独立末lap=-2 · 被審末lap=-2)
```
三本すべてで完全一致:
- `proof/環制御/z再生log.txt` (commit済proof, 900行)
- `proof/環制御/z実機live.txt` (実機325標本, 自身のx/yを源に再計算) → 実機路も θ/r/lap 全行一致
- HEAD再生成 `docs/adversary/証/20260811-梯4丙/z_HEAD_1.txt`
K16 決定論 (梯2側): 同一入力logで 2走 → `diff` 空。commit済proofとも diff空 → 縫目ヒステリシス追加 (`d8a9d1e`) は本入力で出力不変。
**限界 (正直申告)**: 一致は `{:.4}` 量子化logを源とする経路についてのみ。**生 f32 値は一切記録されていない**為、量子化前の真値との差は原理的に測れぬ (A11 の本質は未解消)。

#### 甲.3.3 批側自身の誤検出 (検査器を検査した)
初走で θ不一致 695行/最大 1.378 rad を検出 → 追跡の結果 **批側parserの瑕疵** (死域中の「θ保持」を「環記憶解除」と同時に落としていた)。被審codeは正。修正後 0件。→ 独立実装ほど、まず自具を疑う。

#### 甲.3.4 K8 総角規約 再発狩り (静的走査, 現行code)
- `機関/環音/src/合成.rs`: frame間補間は **総角 (θ+2π·lap) を線形補間→事後に θ/lap へ分解** (`合成.rs:49-50,106`) → 丙-C3 遵守 (合)。θ単体補間の再発 **無**。
- 監視項 (未断定): `音高.rs:52` 十二平均律は `z.theta.rem_euclid(TAU)` を `.round()` → tie-break が浮動小数残差依存 (梯2 丙-D2 と同型)。**梯4 の方位量子化が同型の書き方をした場合は即欠陥として起票する**。

### 甲.4 判定 (現時点)
梯4 = **未着地**。合格判定は **一件も出していない**。着地前に確定した是正要求 = 甲.3.1 (haptic 空振り) 一件。
子鐘: `terra`=実走 (基線, 甲.5) · `solas`=保存則数学 (K1-K4 定式化) — 結果は着次併合。

### 甲.5 基線実走 (子鐘 @terra, 2026-08-11 14:19-14:24 CEST, 書込無·commit無)
`機関/環制御` @ `aacc6e6`: `cargo build` status=0 · `cargo test` **status=0 失敗零** — 46 (lib: polar 11+z 30+入力源 5) + 7 (z統合) + 17 (敌対_z) + 4 (敌対_丙_a6) + 3 (敌対_丙_検算) + 13 (敌対_極) = **90 passed / 0 failed**。
`cargo test -- --ignored` = 全target 0 tests → 一季の `#[ignore]` 契約形 5件は **是正済みで解除され常時緑側へ移った** (`f79f470` polar是正) — diskで確認: assertは理想契約のまま、緩めてはいない。
- 注 (欠陥に非ず、命名腐食): `a7_死域境界でrが不連続に跳躍する_既知欠陥` 等 5件は **既に是正済なのにtest名が「既知欠陥」のまま**。契約形/証跡形が両方緑で重複。梯4で同法を使うなら 是正時に名を更める規律を入れよ。
- **`機関/宇宙` は別repo (submodule, gitlink `160000` @ `7babaee`)** — 梯4 が場側を触るなら commitが二本に割れる。子repo未推進のままの親側commit = 再現不能 (一季 欠7 と同型) → **着地時の必須検査項**。`宇宙` 基線: `cargo build` status=0 · `cargo test` status=0 (実走 14:20:18→14:24:04)。

---

## 乙節 — 梯5 (mic→z) 審査

### 乙.0 着地状態 (実測 2026-08-11)
- repo HEAD = `aacc6e6` · `機関/` 実在crate = 世界·世界審·内在·宇宙·宇宙-嵐修·宇宙-老化診断·小殿·環制御·環音·零·零審
- **`機関/歌口` 不在** (`ls` 実測) → 梯5 = **未着地**
- working tree 汚れ: `proof/環音/環音.wav` (M) · `proof/環制御/z実機live.txt` (??) · submodule `実験/第二配線`·`機関/宇宙` (m) · `.gaia/` (??)
- 契約源 (審査基準) = `文書/環統合.md` 界面写像表 音高行 + 実装梯5行 + 丙-C1/C2/C3 · `文書/歌語.md` 四基底
- 判定: 以下は**着地前に登録した**攻撃目録 (①) + 独立注入具 (②)。着地後、同目録を全数実走して 乙.4 に追記する。

### 乙.1 契約の読み (審側の解釈 — 建側と食い違えば其れ自体が欠陥)
`文書/環統合.md` 音高行より、梯5 は 音 → z の**逆写像**:
```
L = log2(f / 基音)   基音=220.0 (梯3 音高.rs 既定と同値, param)
lap = floor(L)                     ← octave = 巻
frac = L − lap ;  θ = 2π·frac      ← 環位置
家  = round(frac·8) mod 8          ← 八家snap (家数=param)
r   = 音量                          ← 振幅
無  = z=0 ⟺ r=0 ∧ lap=0           ← 丙-C1
連続化は 総角 θ+2π·lap で行う       ← 丙-C3
```
→ **梯3 (z→音) と梯5 (音→z) は逆写像対**である。故に最強の検査は往復 (乙.1-R)。

### 乙.2 攻撃目録 (着地前登録 — 着地時に全数実走)

| # | 攻撃面 | 具体反例 (注入波) | 期待契約 |
|---|---|---|---|
| **C1** | 音高精度 | 家梯子16点 (lap0-3 × 家0/1/3/7, 純音2s) | θ誤差·lap誤差を**実測**。家率2^(h/8)からの偏差を cent で申告 |
| **C2** | 巻境界 lap誤判 | 440/880 の ±0/±1/±5 cent (10本) | floor(L) 境界が決定論的。厳密2^k での帰属を契約明記 |
| **C3** | 家境界 tie-break | frac=(2k+1)/16 の8点 (=梯2 丙-D2 202.5°の音版) | 全8点で同一規約 (上位家 or round_ties_even)。一点だけ反転=欠陥 |
| **C4** | 無音→z | 厳密零 2s | **r=0 ∧ lap=0 ∧ z=0**。θ出力=無効印。偶発角=不合格 |
| **C5** | 雑音→偶発z | 白色雑音 (決定論LCG) 振幅0.5/0.05/0.005 | 確度gate必須。雑音に音高を断定したら**不合格** (r=0契約) |
| **C6** | 準無音·非音高 | 50/60Hz hum · DC 0.3 · 振幅1e-4 の440 | 振幅gate下限param。全て 無 へ落ちる事 |
| **C7** | octave誤り (古典致命) | 基音欠 (2f,3f,4f,5f of 220) · 鋸歯220 · 矩形293.66 · 440+弱220混入 | 真の音高を取る事。FFT最大peak追従なら lap±1 誤り→**露呈**する |
| **C8** | 巻境chatter/単調性 | vibrato ±50cent@6Hz (巻内) · ±120cent (巻跨ぎ) · glide 220↔880 (4s) | ±50centで lap不動 · 巻跨ぎは有界±1 (ヒステリシス) · glideで lap単調 · 総角単調 |
| **C9** | 遅延 | 厳密無音1.000s→標本境界で急起440 · 逆(急止) | 実測遅延を**数値で申告**。frame/hop/buffer=param。契約に上限記載 |
| **C10** | amp→r 線形 | 振幅0.25/0.5/1.0 @440 | r比 1:2:4 (梯3 B5の逆). 非線形なら曲線を申告 |
| **C11** | 出現律 | 無音0.5s→100ms線形fade-in→440 | r が連続に登る。階段/瞬時跳躍=**出現律違反** |
| **C12** | 律動 | 2Hz gate 440 (duty0.5) | r が2Hz往復。無音区間で lap記憶が壊れぬ事 (C4と両立) |
| **C13** | 帯域外 | 27.5Hz (lap=−3) · 14080Hz (lap=6) · 21000Hz (Nyquist近傍) | **負lap** の契約有無 · 上限param (fs·0.45, 梯3 欠3と数値一致) · 折返し禁 |
| **C14** | 形式頑健 | 44.1kHz · stereo (L=440/R=660) · 8bit · 振幅1.6 clip | panic禁 · downmix規約が契約に無ければ欠陥 · r上限clamp |
| **C15** | 多声 | 440+660 同振幅 | 単声契約なら決定論的に一方 or 無。実行毎に揺れたら不合格 |
| **C16** | 決定論再生 | 同一wav 2回 → z列 diff空 (bit一致) | 梯2 A10 と同法 |
| **C17** | **z出力log形式互換** | 歌口z log vs `proof/環制御/z再生log.txt` を diff | 梯2 形式 `Z ts= x= y= theta= r= lap=` + `# param …` header と**同一**。別形式=下流分裂 |
| **C17b** | 下流消費可能性 | 歌口 log を `機関/環音/src/源.rs` に食わせる | 環音 源.rs は梯1 `TICK … L(angle= mag=)` を読む。歌口が Z行のみ出すなら**梯5→梯3が繋がらない** (要確認) |
| **C18** | hardcode狩 | 標本率·FFT長·hop·窓·確度閾·基音220·A4=440·家数8·振幅gate·下限/上限Hz | 鉄則: 全param既定つき (例外=LOVE=1)。裸literal=欠陥 |
| **C19** | param網羅 | CLI露出 + 起動log の `# param …` 一行 | 梯2/梯3同法。露出無きparamは審査不能→UNVERIFIED |
| **C20** | 規約三重実装 | 歌口の家番号算出 vs `機関/環制御 契約層` (B9一本化 `180d640`) | B9で契約層へ集約済。歌口が再実装したら**B9退行** |
| **C21** | 実mic | CoreAudio 実捕捉 | wav file路のみで「動く」=**UNVERIFIED降格**。実mic proof (録音+z log+壁時計) 必須 |
| **C22** | 自test/build | `cargo build && cargo test` を**独立worktreeで再現** | 梯3 欠7 (commit済状態でcompile不能·再現不能な緑主張) の再発検査 |
| **R (往復)** | **逆写像整合** | 梯3 `環音` で z→wav 生成 → 梯5 `歌口` で wav→z 復元 | 元z と復元z が家/巻/θ で一致。梯3·梯5 の**両者が同時に間違っていない**限り通らぬ最強検査 |

### 乙.3 独立注入具 (審具, 着地前に完成·実測済)

| file | 役 |
|---|---|
| `docs/adversary/具/歌口注入生成.py` | 既知信号corpus生成。stdlib のみ · `wave`/numpy不使用の**手書きRIFF** · 被審code非参照 · 期待値は契約式から導出 |
| `docs/adversary/具/注入検証.py` | 生成corpusを **別経路解析器** `wav審.py` (自前FFT, 一季で校正11/11済) で実測→期待値突合 |
| `docs/adversary/具/注入目録.json` | 65件の期待値manifest (freq/L/lap/frac/θ/家/r/註) |
| `docs/adversary/具/注入検証結果.md` | 実測突合表 (下記) |
| `docs/adversary/具/合成歌/*.wav` | 注入波実物 65本 (13MB, 生成器が決定論 → 再生成可) |

**注入波 独立検証 (実走 2026-08-11, `/opt/homebrew/bin/python3`)**:
```
$ python3 具/歌口注入生成.py   → 生成 65 件
$ python3 具/注入検証.py       → 検証 65 件 · 不一致 0
```
音高定義のある全件で 実測周波数 − 期待周波数 が解析分解能以内。**注入波そのものは正しい**が独立に立証された (被審へ入れる前に測定器を測った)。非音高信号 (雑音·無音·DC·多声·倍音族) は支配周波数が無意味な為 判定=—、これらは被審のz出力側で r=0契約・確度gate として判定する。

**この具の限界 (正直申告 — 一季 乙.2 を継承)**:
- 分解能 = fs/N_fft (zero-padding無)。2.0s@48k → 約0.37Hz。内挿値は保証誤差ではない。
- 折返しか真の周波数かは標本列からは原理的に判別不能 (C13の判定は被審の宣言paramとの突合で行う)。
- stereo定位·THD/SNR·上位3peak超の成分は解析対象外。
- **本具は「入力が正しい」しか言わぬ。z出力の正しさは 乙.4 で被審log/API から測る。**

### 乙.4 着地後 実走 (未実施 — 梯5未着地)
> 着地次第、乙.2 の C1–C22 + R を全数実走し、実測値のみで本節を埋める。
> 未実行の項は「無」と書く。緑主張は独立worktree再現を伴う場合のみ採る。

### 乙.5 判定
**保留 (被審未着地)**。現時点で建根乙に対する合否判定は**出していない**。
着地前に確定している事は二つのみ: ①攻撃目録 (乙.2) が登録済 ②注入具が独立検証済 (65/65)。

### 乙.6 手続 (審側の遵守事項)
- 建側 file は触らぬ (一季 甲.2.8 の事故を継承した規律)。測定は `.scratch/` 固定worktree で行い、測定後撤去。
- `/tmp` 禁。審具·証跡は repo 内 `docs/adversary/` 配下のみ。
- 子鐘: `terra`=実走 · `ghoul-sonnet`=信号解析。`naru-gpt` 使用禁 (一季で模型不可を確認済)。
